services:
  dashboard:
    build: .
    container_name: retail-dashboard
    ports:
      - "8501:8501"
    env_file:
      - .env
    command: streamlit run src/dashboard.py --server.address=0.0.0.0
